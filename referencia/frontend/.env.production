# Carregado automaticamente pelo Vite em `npm run build` (modo production).
# A URL da API é pública (embutida no bundle do navegador) — não é segredo.
# Backend Django hospedado no Render.
VITE_API_URL=https://eixo-webgis-api.onrender.com/api/v1
